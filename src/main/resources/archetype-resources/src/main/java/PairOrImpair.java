public class PairOrImpair {

    public static String determine(int number) {
        if (number % 2 == 0) {

            return number + " is a pair number";

        } else {

            return number + " is an impair number";
        }
    }
}
